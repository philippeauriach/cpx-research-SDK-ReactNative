import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    position: "absolute",
  },
  image: {
    height: "100%",
    resizeMode: "cover",
    width: "100%"
  }
});

export default styles;
